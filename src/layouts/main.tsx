import { EarnSection, HeroSection } from "../components"

export const Main = () => {
	return (
		<main>
			<div className="container">
				<HeroSection />
				<EarnSection />
			</div>
		</main>
	)
}
